import { Button, Group } from "@mantine/core";
import QuoteSection from "../lib/sections/quote";
import TextEditor from "../lib/sections/textEditor";

export default function IndexPage() {
  return (
    <TextEditor/>
  );
}
